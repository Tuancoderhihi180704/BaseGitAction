name: Hello World

on:
  push:
    branches: [main]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "TODAY=$(date +%y-%m-%d)" >> $GITHUB_ENV
        run: echo Hello World,to day is $TODAY